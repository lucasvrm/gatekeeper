# User Prompt - Micro-Task Executor

Execute the development plan with the following parameters:

```
OUTPUT_ID: {{OUTPUT_ID}}
PROJECT_PATH: {{PROJECT_PATH}}
```

Begin by checking prerequisites, then proceed through all phases.

---

## Optional Overrides

Uncomment and modify any of the lines below to customize behavior for this session:

```
# MAX_CORRECTION_ATTEMPTS: 5        (default: 3)
# MAX_FILES_PER_TASK: 3             (default: 2)
# MAX_DEPENDENCY_READS: 8           (default: 5)
# SKIP_PHASE_3: true                (default: false)
# BASE_REF: origin/develop          (default: origin/main)
# GATEKEEPER_URL: http://host:4000  (default: http://localhost:3000)
```
